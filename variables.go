package main

import "fmt"

func main() {
	var hello string = "Hello"
	var number uint8 = 255
	var new_number uint8 = 200
	var sum uint8 = number + new_number
	fmt.Println(sum)
	fmt.Println(hello)
	fmt.Println(number + new_number)
}
